scroll-x: hidden;

addEventListener("mousewheel", e => e.preventDefault())

element.scrollBy(xScroll, yScroll)


clearInterval(setIntervalVariableName) --> To clear a running interval (Put this inside a conditional block)


function isInViewport(element) {
    const rect = element.getBoundingClientRect();
    return (
        rect.top >= 0 &&
        rect.left >= 0 &&
        rect.bottom <= (window.innerHeight || document.documentElement.clientHeight) &&
        rect.right <= (window.innerWidth || document.documentElement.clientWidth)
    );
}

flex: display;
flex-wrap: wrap;


UI

Font
Pop ups
Background (Web)

Functionalities

Main screen

Arcade mode -
Missing a black tile should show error screen
Clicking on a white tile """ ""
Remove black from yellow
Add score to UI
localstorage, check if currentScore is greater than high score value and then replace

add event listeners on keys
polish UI
fix end tile 
pause at start


screens -
Game over screen
Best score beaten screen


Different modes
